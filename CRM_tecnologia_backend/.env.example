# Base de Datos (Opcional - por defecto intentará conectar a PostgreSQL/Supabase y usará SQLite local si no está disponible)
# DATABASE_URL=postgresql+psycopg2://postgres:crm89SeM#12@db.ctvzryjpycworkkaarkx.supabase.co:5432/postgres
# DATABASE_URL=sqlite:///./crm.db

# Configuración de Servidor
PORT=8000
HOST=127.0.0.1

# Seguridad y Autenticación OTP / JWT
JWT_SECRET_KEY=cambia-esta-clave-en-produccion-123456
ACCESS_TOKEN_EXPIRE_MINUTES=60
OTP_EXPIRATION_MINUTES=10

# Configuración SMTP para envío de correos OTP (Opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USE_SSL=true
EMAIL_USER=eduardocaballero392@gmail.com
EMAIL_PASSWORD=
